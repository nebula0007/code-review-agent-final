[project]
name = "code-review-agent"
version = "0.1.0"
description = "Code review RL environment"
authors = ["Ishant"]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openai"
]

[tool.setuptools]
packages = ["app"]